"use client";

/**
 * Map Provider Configuration
 * 
 * The application uses 2GIS Maps API as the primary map provider.
 * If the 2GIS API key is not provided, it falls back to OpenStreetMap (Leaflet).
 * 
 * Ensure VITE_2GIS_MAP_KEY is set in your .env file for 2GIS integration.
 */