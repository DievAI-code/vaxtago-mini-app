"use client";

/**
 * 2GIS Maps Configuration
 * The application exclusively uses 2GIS Maps SDK for map rendering and searching.
 */
export const MAPS_CONFIG = {
  defaultCenter: [41.2995, 69.2401] as [number, number], // Tashkent
  defaultZoom: 12,
  provider: "2gis",
};